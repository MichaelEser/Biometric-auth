import React from "react";
// Root component
// Public routes:    /login, /register
// Protected routes: /dashboard  (wrapped in AuthGuard)
export default function App() {
  return <div>Biometric Auth</div>;
}
