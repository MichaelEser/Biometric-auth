// Shared TypeScript interfaces used across the whole app
//
// User            { id, email, username, createdAt }
// TokenResponse   { accessToken, refreshToken, tokenType }
// VerifyResponse  { authenticated, similarityScore }
// ApiError        { detail: string }
